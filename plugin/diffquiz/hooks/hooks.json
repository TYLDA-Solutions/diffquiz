{
  "description": "diffquiz auto mode: intercepts git push / gh pr create so the author is quizzed on their diff first (non-blocking — fails open on any error).",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks/pre-push-quiz.mjs\"",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
